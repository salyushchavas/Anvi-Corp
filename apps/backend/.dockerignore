# Build output — rebuilt inside the image.
target/

# Locally-uploaded files. These are real user documents when they exist;
# they must never be baked into an image.
uploads/

# Editor / OS noise
.idea/
.vscode/
*.iml
.DS_Store

# Never copy real secrets into a build context.
.env
.env.*
!.env.example
