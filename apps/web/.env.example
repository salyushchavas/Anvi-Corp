# Contact form (Resend)
# Get an API key at https://resend.com/api-keys
RESEND_API_KEY=

# Where contact-form submissions should land
MAIL_TO=info@anvicorp.com

# "From" sender. Until anvicorp.com is verified in Resend (DNS records),
# use Resend's onboarding sender for the Vercel preview:
#   MAIL_FROM=Anvi Corp Website <onboarding@resend.dev>
# After verifying the domain in Resend, switch to:
#   MAIL_FROM=Anvi Corp Website <noreply@anvicorp.com>
MAIL_FROM=Anvi Corp Website <onboarding@resend.dev>

# ── Internal web-mail (the /mail app) ────────────────────────────────────────
# Base URL of the A1–A3 mail backend (com.anvicorp.api on Railway). Leave blank
# for same-origin. NEXT_PUBLIC_* are inlined into the client bundle at build.
NEXT_PUBLIC_API_URL=https://api.anvicorp.com
# Display name for the mail app header ("Anvi <name>"). Default: Mail
NEXT_PUBLIC_APP_NAME=Mail
