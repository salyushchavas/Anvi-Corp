'use client';

import RecordingGallery from '@/components/recordings/RecordingGallery';

export default function ManagerRecordingGalleryPage() {
  return (
    <RecordingGallery
      title="Recording Gallery"
      subtitle="Every intern's interview + monthly evaluation recordings (view-only). Approvals live under Recording Approvals."
    />
  );
}
